import 'package:flutter/material.dart';

class DoctorChat extends StatelessWidget {
  const DoctorChat({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('DoctorChat'),
    );
  }
}